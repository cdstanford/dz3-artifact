(declare-fun field_date () String)
(assert (let ((a!1 (re.++ (re.union (re.range "A" "Z") (re.range "a" "z"))
                  (re.++ (str.to.re "-")
                         (re.++ (re.range "0" "9") (re.range "0" "9")))))
      (a!5 (re.++ (str.to.re "2")
                  (re.++ (str.to.re "0")
                         (re.++ (str.to.re "1") (str.to.re "9")))))
      (a!6 (re.++ (str.to.re "2")
                  (re.++ (str.to.re "0")
                         (re.++ (str.to.re "2") (str.to.re "0"))))))
(let ((a!2 (re.++ (re.union (re.range "A" "Z") (re.range "a" "z"))
                  (re.++ (re.union (re.range "A" "Z") (re.range "a" "z")) a!1))))
(let ((a!3 (re.++ (re.range "0" "9")
                  (re.++ (re.range "0" "9") (re.++ (str.to.re "-") a!2)))))
(let ((a!4 (str.in.re field_date
                      (re.++ (re.range "0" "9") (re.++ (re.range "0" "9") a!3)))))
  (and a!4
       (or (str.in.re field_date (re.++ re.all a!5))
           (str.in.re field_date (re.++ re.all a!6)))))))))

(check-sat)